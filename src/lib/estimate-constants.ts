export const ESTIMATE_STATUS_LABELS: Record<string, string> = {
  draft: "작성중",
  confirmed: "확정",
  sent: "발송완료",
};

export const ESTIMATE_STATUS_VARIANTS: Record<string, "outline" | "default" | "secondary"> = {
  draft: "outline",
  confirmed: "default",
  sent: "secondary",
};

export const VAT_RATE = 0.1;

export const LOCK_TIMEOUT_MINUTES = 5;
