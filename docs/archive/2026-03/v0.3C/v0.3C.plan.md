# v0.3C Plan — 데일리 스크럼 시스템

## Executive Summary

| 관점 | 내용 |
|------|------|
| **Problem** | 팀원의 일일 업무 계획 도구가 없어 오늘 할 일 파악, 우선순위, 시간 배분이 개인 기억에 의존한다 |
| **Solution** | AI 대화형 3단계 스크럼 플로우(브레인스토밍 → 우선순위 → 시간 배치) + 건너뛰기 + 마감 회고 시스템 |
| **Function UX Effect** | 매일 아침 3~5분 내 하루 계획 수립, AI가 이월/마감 항목을 자동 제안하여 누락 방지 |
| **Core Value** | 개인 생산성 극대화 + PM의 팀 상황 실시간 파악 → 프로젝트 지연 조기 감지 |

---

## 1. 배경 및 목표

### 1.1 현재 상태
- 데일리 스크럼 모듈: **미구현** (v0.3 Phase C에 해당)
- v0.3A: 프로젝트 리워크 + TASK 모듈 + 의존성 맵 + AI 리뷰 ✅
- v0.3B: 킥오프 시스템 + 일정 캘린더 ✅
- Task에 `source: "scrum"` 타입은 이미 정의됨 (`src/types/task.types.ts`)
- AI 인프라: Gemini 2.0 Flash 클라이언트 구축 완료 (`src/lib/ai/gemini-client.ts`)
- 일정 캘린더: FullCalendar 기반 `/schedule` 페이지 완료

### 1.2 목표
매일 아침 **"오늘 하루를 설계"**하는 AI 대화형 데일리 스크럼 시스템 구축

### 1.3 참조
- 마스터 계획서: `workspace/GRIFF-Q-0.3-plan.md` Phase C (827~963줄)
- 시나리오: `workspace/GRIFF-Q-0.3-plan.md` S-C1 ~ S-C3 (1745~1902줄)

---

## 2. 기능 범위

### 2.1 In-Scope (v0.3C)

| # | 기능 | 우선순위 |
|---|------|---------|
| 1 | DB 스키마: `daily_scrums` + `scrum_items` 테이블 | P0 |
| 2 | 3단계 스텝 UI 프레임 + 스텝 인디케이터 | P0 |
| 3 | A단계: AI 대화형 브레인스토밍 (Gemini) | P0 |
| 4 | A단계: 자동 이월 (어제 미완료 Task 제안) | P0 |
| 5 | A단계: 자유 입력 모드 (AI 없이 직접 추가) | P0 |
| 6 | B단계: 드래그 정렬 우선순위 (dnd-kit) | P0 |
| 7 | B단계: 아이젠하워 매트릭스 뷰 (선택 토글) | P1 |
| 8 | C단계: 타임블록 배치 (09:00~18:00 타임라인) | P0 |
| 9 | C단계: GRIFF 스케줄 연동 (겹침 방지) | P1 |
| 10 | C단계: 과부하 경고 (할당 > 가용) | P1 |
| 11 | 스크럼 완료 → Task 자동 등록/매핑 | P0 |
| 12 | 건너뛰기 기능 (사유 선택) | P0 |
| 13 | 하루 마감 회고 (완료 체크 + 미완료 이월) | P1 |
| 14 | 스크럼 이력 페이지 | P1 |
| 15 | 사이드바 메뉴에 "데일리 스크럼" 추가 | P0 |

### 2.2 Out-of-Scope (후속 버전)

| 기능 | 미룬 이유 | 버전 |
|------|----------|------|
| PM 리마인드 알림 발송 | 인앱 알림 시스템 (Phase D) 선행 필요 | v0.3D |
| 대시보드 스크럼 위젯 | 대시보드 개편 (v0.4) | v0.4 |
| 달성률 → 퍼포먼스 연동 | 퍼포먼스 모듈 (v0.6) | v0.6 |
| 미시작 에스컬레이션 | 인앱 알림 시스템 필요 | v0.3D |

---

## 3. DB 스키마

### 3.1 daily_scrums 테이블

```sql
CREATE TABLE daily_scrums (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL REFERENCES auth.users(id),
  scrum_date date NOT NULL,
  status text NOT NULL DEFAULT 'not_started'
    CHECK (status IN ('not_started', 'brainstorming', 'prioritizing', 'scheduling', 'completed', 'skipped')),
  skip_reason text,
  ai_conversation jsonb DEFAULT '[]',
  completed_at timestamptz,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now(),
  UNIQUE(user_id, scrum_date)
);

CREATE INDEX idx_scrums_user_date ON daily_scrums(user_id, scrum_date);
```

### 3.2 scrum_items 테이블

```sql
CREATE TABLE scrum_items (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  scrum_id uuid NOT NULL REFERENCES daily_scrums(id) ON DELETE CASCADE,
  title text NOT NULL,
  priority text DEFAULT 'normal'
    CHECK (priority IN ('urgent', 'important', 'normal', 'later')),
  priority_order integer DEFAULT 0,
  time_block_start time,
  time_block_end time,
  estimated_minutes integer DEFAULT 30,
  is_carried_over boolean DEFAULT false,
  source_task_id uuid REFERENCES tasks(id) ON DELETE SET NULL,
  generated_task_id uuid REFERENCES tasks(id) ON DELETE SET NULL,
  status text DEFAULT 'planned'
    CHECK (status IN ('planned', 'completed', 'skipped')),
  sort_order integer DEFAULT 0,
  created_at timestamptz DEFAULT now()
);

CREATE INDEX idx_scrum_items_scrum ON scrum_items(scrum_id);
```

### 3.3 RLS 정책

```sql
-- daily_scrums: 본인만 CRUD
POLICY "scrums_select" FOR SELECT USING (user_id = auth.uid() OR
  EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role IN ('super', 'boss', 'manager')));
POLICY "scrums_insert" FOR INSERT WITH CHECK (user_id = auth.uid());
POLICY "scrums_update" FOR UPDATE USING (user_id = auth.uid());

-- scrum_items: 스크럼 소유자만
POLICY "scrum_items_select" FOR SELECT USING (
  EXISTS (SELECT 1 FROM daily_scrums WHERE id = scrum_id AND (
    user_id = auth.uid() OR EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role IN ('super', 'boss', 'manager'))
  )));
POLICY "scrum_items_all" FOR ALL USING (
  EXISTS (SELECT 1 FROM daily_scrums WHERE id = scrum_id AND user_id = auth.uid()));
```

---

## 4. 타입 정의

### 4.1 `src/types/scrum.types.ts`

```typescript
export type ScrumStatus = "not_started" | "brainstorming" | "prioritizing" | "scheduling" | "completed" | "skipped";
export type ScrumItemPriority = "urgent" | "important" | "normal" | "later";
export type ScrumItemStatus = "planned" | "completed" | "skipped";

export interface DailyScrum {
  id: string;
  user_id: string;
  scrum_date: string;
  status: ScrumStatus;
  skip_reason: string | null;
  ai_conversation: AiMessage[];
  completed_at: string | null;
  created_at: string;
  updated_at: string;
}

export interface ScrumItem {
  id: string;
  scrum_id: string;
  title: string;
  priority: ScrumItemPriority;
  priority_order: number;
  time_block_start: string | null;
  time_block_end: string | null;
  estimated_minutes: number;
  is_carried_over: boolean;
  source_task_id: string | null;
  generated_task_id: string | null;
  status: ScrumItemStatus;
  sort_order: number;
}

export interface AiMessage {
  role: "user" | "assistant";
  content: string;
  timestamp: string;
}

export const PRIORITY_CONFIG: Record<ScrumItemPriority, { label: string; color: string; icon: string }> = {
  urgent: { label: "긴급", color: "text-red-600", icon: "🔴" },
  important: { label: "중요", color: "text-orange-500", icon: "🟠" },
  normal: { label: "일반", color: "text-gray-500", icon: "⚪" },
  later: { label: "후순위", color: "text-blue-500", icon: "🔵" },
};

export const ESTIMATED_TIME_OPTIONS = [
  { value: 15, label: "15분" },
  { value: 30, label: "30분" },
  { value: 60, label: "1시간" },
  { value: 120, label: "2시간" },
  { value: 240, label: "반나절" },
];

export const SKIP_REASONS = [
  "외근",
  "휴가",
  "회의 밀집",
  "기타",
];
```

---

## 5. 페이지 구성

```
src/app/(dashboard)/scrum/
├── page.tsx              # 오늘 스크럼 메인 (진행/완료/건너뛰기)
├── loading.tsx           # 로딩
├── history/
│   ├── page.tsx          # 스크럼 이력 목록
│   └── [date]/
│       └── page.tsx      # 특정 날짜 상세
```

---

## 6. 컴포넌트 설계

```
src/components/scrum/
├── scrum-stepper.tsx           # 3단계 스텝 인디케이터 (A → B → C)
├── scrum-brainstorm.tsx        # A단계: AI 대화 + 자유 입력 + 이월 제안
├── scrum-ai-chat.tsx           # AI 대화 인터페이스 (Gemini)
├── scrum-prioritize.tsx        # B단계: 드래그 정렬 + 우선순위 라벨
├── scrum-eisenhower.tsx        # B단계: 아이젠하워 매트릭스 2×2 뷰
├── scrum-schedule.tsx          # C단계: 타임블록 배치 타임라인
├── scrum-timeline.tsx          # C단계: 09:00~18:00 시각 타임라인
├── scrum-summary.tsx           # 완료 요약 뷰
├── scrum-skip-dialog.tsx       # 건너뛰기 다이얼로그 (사유 선택)
├── scrum-review.tsx            # 하루 마감 회고
└── scrum-carry-over.tsx        # 이월 항목 카드
```

---

## 7. Server Actions

### 7.1 `src/actions/scrum.ts`

| Action | 설명 |
|--------|------|
| `getOrCreateTodayScrum()` | 오늘 스크럼 조회, 없으면 생성 |
| `updateScrumStatus(id, status)` | 스크럼 단계 진행 |
| `skipScrum(id, reason)` | 스크럼 건너뛰기 |
| `addScrumItem(scrumId, data)` | 스크럼 항목 추가 |
| `updateScrumItem(itemId, data)` | 항목 수정 (우선순위/시간배치) |
| `deleteScrumItem(itemId)` | 항목 삭제 |
| `reorderScrumItems(scrumId, itemIds)` | 항목 정렬 순서 변경 |
| `completeScrumItem(itemId)` | 항목 완료 처리 |
| `completeScrum(id)` | 스크럼 완성 → Task 자동 등록 |
| `getCarryOverItems(userId)` | 어제 미완료 항목 조회 |
| `getScrumHistory(userId, limit)` | 스크럼 이력 조회 |
| `reviewDay(scrumId, completedIds, notes)` | 하루 마감 회고 처리 |

### 7.2 AI API Route

```
src/app/api/scrum/chat/route.ts   # POST: AI 대화형 브레인스토밍
```

Gemini 2.0 Flash 사용 (기존 `gemini-client.ts` 재사용):
- 시스템 프롬프트: 할 일 수집 도우미 역할
- 컨텍스트: 어제 미완료 Task, 오늘 마감 Task, 오늘 일정
- 응답: 자연어 + 구조화된 할 일 추출

---

## 8. 3단계 플로우 상세

### 8.1 A단계: 브레인스토밍 — "오늘 뭘 해야 하지?"

**입력 방식 2가지:**
1. **AI 대화형**: Gemini와 자연어 대화로 할 일 추출
2. **자유 입력**: AI 없이 직접 항목 추가 (토글 전환)

**자동 이월:**
- 어제 미완료 Task (source: scrum/manual) 자동 표시
- "어제 남은 항목이 있어요. 오늘 이어서 할까요?" AI 제안
- 체크하면 `is_carried_over: true`로 추가

**마감 임박 Task 표시:**
- D-2 이내 마감 Task 자동 표시

### 8.2 B단계: 우선순위 — "뭐가 더 급하지?"

- dnd-kit 드래그 정렬 (기존 체크리스트와 동일 패턴)
- 4단계 우선순위 라벨: 긴급(빨강) / 중요(주황) / 일반(회색) / 후순위(파랑)
- 아이젠하워 매트릭스 2×2 토글 뷰 (선택적)

### 8.3 C단계: 시간 배치 — "언제 하지?"

- 09:00~18:00 타임라인 (세로 그리드)
- 항목을 시간 슬롯에 드래그 배치
- 예상 소요시간: 15분 / 30분 / 1시간 / 2시간 / 반나절
- GRIFF 스케줄에서 오늘 일정 자동 블록 표시
- 과부하 경고: 할당시간 합 > 가용시간 시 경고

### 8.4 완료 후 자동화

- 각 항목 → Task 자동 생성 또는 기존 Task와 매핑
  - `source_task_id` 있으면 → 기존 Task 상태 갱신
  - `source_task_id` 없으면 → 새 Task 생성 (`source: "scrum"`)
- `generated_task_id`에 생성/연결된 Task ID 기록

---

## 9. 의존성

### 9.1 기존 패키지 (추가 설치 불필요)
- `@dnd-kit/core` + `@dnd-kit/sortable` — 이미 설치 (킥오프 체크리스트)
- `@google/generative-ai` — 이미 설치 (AI 리뷰)
- `@fullcalendar/react` — 이미 설치 (일정 캘린더)
- `date-fns` — 이미 설치

### 9.2 신규 패키지 없음

---

## 10. 구현 순서

| Step | 작업 | 의존성 |
|------|------|--------|
| 1 | DB 마이그레이션: `daily_scrums` + `scrum_items` (RLS, 트리거) | - |
| 2 | 타입 정의: `scrum.types.ts` | Step 1 |
| 3 | Server Actions: `scrum.ts` (12개 함수) | Step 2 |
| 4 | AI API Route: `/api/scrum/chat` (Gemini 연동) | Step 3 |
| 5 | 스텝 인디케이터 + 페이지 프레임 | Step 3 |
| 6 | A단계: 브레인스토밍 (AI 대화 + 자유 입력 + 이월) | Step 4, 5 |
| 7 | B단계: 우선순위 정렬 (dnd-kit + 라벨) | Step 6 |
| 8 | B단계: 아이젠하워 매트릭스 뷰 | Step 7 |
| 9 | C단계: 타임블록 배치 (타임라인 UI) | Step 7 |
| 10 | C단계: 스케줄 연동 + 과부하 경고 | Step 9 |
| 11 | 스크럼 완료 → Task 자동 등록 로직 | Step 9 |
| 12 | 건너뛰기 기능 (다이얼로그 + 사유) | Step 5 |
| 13 | 하루 마감 회고 UI | Step 11 |
| 14 | 스크럼 이력 페이지 | Step 3 |
| 15 | 사이드바 메뉴에 "데일리 스크럼" 추가 | - |
| 16 | 빌드 검증 + 브라우저 QA (4조합) | All |
| 17 | 이슈 수정 반복 (문제 0건까지) | Step 16 |

---

## 11. 영향받는 기존 파일

| 파일 | 변경 내용 |
|------|----------|
| `src/lib/constants.ts` | 사이드바에 "데일리 스크럼" 메뉴 추가 |
| `src/lib/ai/gemini-client.ts` | 스크럼용 모델 인스턴스 추가 (옵션) |
| `src/actions/task.ts` | 스크럼 완료 시 Task 자동 생성 함수 연동 |
| `src/types/task.types.ts` | `source: "scrum"` 이미 정의됨 (변경 없음) |
| `package.json` | 추가 패키지 없음 (변경 없음) |

---

## 12. 테스트 계획

### 12.1 테스트 루틴
```
[구현 완료] → [next build 0 에러] → [브라우저 4조합 테스트]
→ [이슈?] → YES: 수정 후 재테스트 / NO: PASS
```

### 12.2 테스트 시나리오

| 시나리오 | 검증 항목 |
|----------|----------|
| 스크럼 시작 | 오늘 날짜 스크럼 자동 생성, 3단계 스텝 표시 |
| A단계: AI 대화 | Gemini 응답 정상, 할 일 항목 추출 |
| A단계: 자유 입력 | 직접 항목 추가/삭제 |
| A단계: 이월 | 어제 미완료 Task 표시 + 체크하면 추가 |
| B단계: 드래그 정렬 | dnd-kit 정상, 우선순위 라벨 변경 |
| B단계: 매트릭스 | 2×2 그리드에 항목 배치 |
| C단계: 타임블록 | 시간 슬롯 배치, 드래그 이동 |
| C단계: 스케줄 연동 | 오늘 일정 블록 표시, 겹침 방지 |
| C단계: 과부하 경고 | 총 시간 초과 시 경고 메시지 |
| 스크럼 완료 | Task 자동 생성/매핑 확인 |
| 건너뛰기 | 사유 저장, 상태 skipped |
| 하루 마감 회고 | 완료/미완료 체크, 미완료 이월 |
| 스크럼 이력 | 날짜별 이력 목록, 상세 조회 |
| 모바일 | 375px에서 3단계 UI 정상 |
| 다크모드 | 전체 다크모드 정상 |

---

## 13. 완료 기준

- [ ] daily_scrums + scrum_items 테이블 생성 (RLS 포함)
- [ ] 3단계 스텝 UI 정상 동작 (A → B → C)
- [ ] A단계: AI 대화형 브레인스토밍 + 자유 입력 + 이월
- [ ] B단계: 드래그 정렬 + 우선순위 라벨 + 매트릭스 뷰
- [ ] C단계: 타임블록 배치 + 스케줄 연동 + 과부하 경고
- [ ] 스크럼 완료 시 Task 자동 등록
- [ ] 건너뛰기 기능 (사유 포함)
- [ ] 하루 마감 회고
- [ ] 스크럼 이력 페이지
- [ ] 사이드바 메뉴 추가
- [ ] 라이트/다크 × 모바일/데스크톱 4조합 정상
- [ ] `next build` 에러 0건
