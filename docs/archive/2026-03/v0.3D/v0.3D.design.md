# v0.3D Design Document — 시스템 통합 + 권한 + 알림 + QA

> **Summary**: v0.3A~C 모듈을 시스템 전체로 통합 — 권한 강화, 사이드바 3그룹, 글로벌 검색 확장, 알림 트리거 확장, 크로스 모듈 연동
>
> **Project**: GRIFF-Q
> **Version**: 0.3D
> **Author**: Claude (PDCA)
> **Date**: 2026-03-07
> **Status**: Draft
> **Planning Doc**: [v0.3D.plan.md](../01-plan/features/v0.3D.plan.md)

---

## 1. Overview

### 1.1 Design Goals
- 기존 4단계 권한(`super/boss/manager/normal`)을 RLS + Server Action + UI에 일관 적용
- 평면 사이드바 → 접이식 3그룹(핵심/지원/설정) 리팩토링
- 기존 SearchBar에 프로젝트/태스크/일정/팀원 검색 추가
- 기존 NotificationBell 인프라에 8종 알림 트리거 추가
- 회의록 ↔ 태스크, 회고 ↔ 프로젝트 양방향 연동
- 대시보드에 v0.3 모듈 위젯 4종 추가

### 1.2 Design Principles
- **기존 코드 최대 활용**: NotificationBell, SearchBar, auth.types.ts 등 이미 구현된 인프라 확장
- **병렬 작업 안전성**: 에이전트별 담당 파일을 엄격히 분리하여 충돌 방지
- **점진적 적용**: RLS 정책을 개별 테이블에 순차 적용, 기존 동작 깨지지 않도록

---

## 2. 기존 인프라 현황 분석

> 기존에 이미 구현된 것들을 정확히 파악하여 중복 구현 방지

| 컴포넌트 | 현재 상태 | v0.3D 변경 |
|----------|----------|-----------|
| `auth.types.ts` | 4단계 역할 + `hasMinimumRole()` ✅ | 변경 없음 |
| `constants.ts` | 평면 나열 11개 + `minRole` 필터링 ✅ | 3그룹 구조 변경 |
| `app-sidebar.tsx` | 단일 "메뉴" 그룹 + Collapsible children ✅ | 3그룹 렌더링 |
| `notification-bell.tsx` | 벨 + 드롭다운 + 읽음/안읽음 + 폴링 ✅ | 변경 없음 (트리거만 추가) |
| `notifications.ts` | `createNotification()` + 마감 알림 ✅ | 8종 트리거 함수 추가 |
| `search-bar.tsx` | Cmd+K + Dialog + 4종 검색 ✅ | 프로젝트/태스크/일정/팀원 추가 |
| `app-header.tsx` | SearchBar + NotificationBell + ThemeToggle ✅ | 변경 없음 |
| `dashboard/page.tsx` | 요약카드4 + 차트3 + 테이블2 + 타임라인1 ✅ | 위젯 4종 추가 (row 삽입) |

---

## 3. D1: 시스템 권한 4단계

### 3.1 RLS 정책 설계

#### projects 테이블 — 역할별 접근

```sql
-- 기존 정책 대체: 역할별 프로젝트 접근
DROP POLICY IF EXISTS "projects_select" ON projects;
CREATE POLICY "projects_select" ON projects FOR SELECT USING (
  -- super/boss: 전체 열람
  EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role IN ('super', 'boss'))
  OR
  -- manager: 본인 생성 + 멤버인 프로젝트
  (EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'manager') AND (
    created_by = auth.uid() OR
    EXISTS (SELECT 1 FROM project_members WHERE project_id = projects.id AND user_id = auth.uid())
  ))
  OR
  -- normal: 멤버인 프로젝트만
  EXISTS (SELECT 1 FROM project_members WHERE project_id = projects.id AND user_id = auth.uid())
);
```

#### tasks 테이블 — 역할별 접근

```sql
DROP POLICY IF EXISTS "tasks_select" ON tasks;
CREATE POLICY "tasks_select" ON tasks FOR SELECT USING (
  -- super/boss: 전체 열람
  EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role IN ('super', 'boss'))
  OR
  -- manager: 담당 프로젝트 태스크 + 본인 태스크
  (EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'manager') AND (
    assignee_id = auth.uid() OR created_by = auth.uid() OR
    EXISTS (SELECT 1 FROM project_members WHERE project_id = tasks.project_id AND user_id = auth.uid())
  ))
  OR
  -- normal: 본인 태스크만
  assignee_id = auth.uid() OR created_by = auth.uid()
);
```

#### daily_scrums 테이블 — (이미 적용됨, 변경 없음)

기존 RLS: 본인 + 관리자 이상 열람 → **유지**

### 3.2 프로젝트 소프트 삭제

```sql
-- projects 테이블 컬럼 추가
ALTER TABLE projects ADD COLUMN IF NOT EXISTS deleted_at timestamptz;
ALTER TABLE projects ADD COLUMN IF NOT EXISTS deleted_by uuid REFERENCES auth.users(id);

-- 삭제 정책: PM(본인 생성) / 대표만
-- Server Action에서 권한 체크 후 soft delete
```

### 3.3 Server Action 권한 미들웨어

**신규 파일**: `src/lib/auth-guard.ts`

```typescript
import { createClient } from "@/lib/supabase/server";
import { type UserRole, hasMinimumRole } from "@/types/auth.types";

export async function requireRole(minRole: UserRole): Promise<{
  userId: string;
  role: UserRole;
  name: string;
}> {
  const supabase = await createClient();
  const { data: { user } } = await supabase.auth.getUser();
  if (!user) throw new Error("인증 필요");

  const { data: profile } = await supabase
    .from("users")
    .select("role, name")
    .eq("id", user.id)
    .single();

  if (!profile || !hasMinimumRole(profile.role as UserRole, minRole)) {
    throw new Error("권한이 부족합니다");
  }

  return { userId: user.id, role: profile.role as UserRole, name: profile.name };
}
```

### 3.4 UI 조건부 렌더링

| UI 요소 | 조건 | 적용 위치 |
|---------|------|----------|
| 프로젝트 삭제 버튼 | `super` 또는 (PM + 본인 생성) | `project-detail-header.tsx` |
| 프로젝트 승인 버튼 | `super` / `boss` | `project-detail-header.tsx` |
| 전사 현황 대시보드 | `super` / `boss` | `dashboard/page.tsx` |
| 팀원 스크럼 열람 | `manager` 이상 | `scrum/history/page.tsx` |

### 3.5 수정 파일 목록 (Agent-1)

| 파일 | 변경 |
|------|------|
| `supabase/migrations/020_role_permissions.sql` | **신규** — RLS + soft delete |
| `src/lib/auth-guard.ts` | **신규** — `requireRole()` |
| `src/actions/project.ts` | `deleteProject()` 추가, 권한 체크 삽입 |
| `src/actions/task.ts` | 수정 시 권한 체크 삽입 |
| `src/components/projects/project-detail-header.tsx` | 삭제/승인 버튼 조건부 렌더링 |

---

## 4. D2: 대시보드 위젯 추가

### 4.1 위젯 배치 — 기존 레이아웃에 삽입

현재 대시보드 구조:
```
Row 0: 인사말 + 날짜
Row 1: QuickActions
Row 2: 요약카드 4개 (마감임박/입금대기/최근회의/프로젝트)
Row 3: 차트 3개 (입금추이/견적상태/회고제출)
Row 4: 테이블 2개 + 타임라인
Row 5: 회의록 + 회고 현황
```

**변경 후**:
```
Row 0: 인사말 + 날짜
Row 1: QuickActions
★ Row 2 (신규): v0.3 위젯 4개 (스크럼상태/오늘태스크/프로젝트진행률/오늘일정)
Row 3: 요약카드 4개 (기존 Row 2)
Row 4: 차트 3개 (기존 Row 3)
Row 5: 테이블 2개 + 타임라인 (기존 Row 4)
Row 6: 회의록 + 회고 현황 (기존 Row 5)
```

### 4.2 위젯 컴포넌트 설계

#### 4.2.1 스크럼 상태 위젯

**파일**: `src/components/dashboard/dashboard-scrum-widget.tsx`

```typescript
interface DashboardScrumWidgetProps {
  scrumStatus: "not_started" | "brainstorming" | "prioritizing" | "scheduling" | "completed" | "skipped" | null;
  itemCount: number;
}
```

| 상태 | 표시 |
|------|------|
| `null` (미시작) | "오늘 스크럼을 시작하세요" + 바로가기 버튼 |
| `brainstorming~scheduling` | "진행 중 (N단계)" + 이어하기 버튼 |
| `completed` | "완료 (N건 계획)" + 확인 링크 |
| `skipped` | "건너뜀" (회색) |

#### 4.2.2 오늘의 태스크 위젯

**파일**: `src/components/dashboard/dashboard-tasks-widget.tsx`

```typescript
interface DashboardTasksWidgetProps {
  tasks: {
    id: string;
    title: string;
    status: string;
    priority: string;
    due_date: string;
  }[];
}
```

- 오늘 마감 태스크 최대 5건 표시
- 상태 아이콘 + 제목 + 우선순위 뱃지
- "전체 보기" 링크 → `/tasks`

#### 4.2.3 프로젝트 진행률 위젯

**파일**: `src/components/dashboard/dashboard-projects-widget.tsx`

```typescript
interface DashboardProjectsWidgetProps {
  projects: {
    id: string;
    name: string;
    progress: number;
    status: string;
  }[];
}
```

- 활성 프로젝트 진행률 바 (최대 4개)
- 프로젝트명 + progress% + 상태 뱃지
- "전체 보기" 링크 → `/projects`

#### 4.2.4 오늘 일정 위젯

**파일**: `src/components/dashboard/dashboard-schedule-widget.tsx`

```typescript
interface DashboardScheduleWidgetProps {
  schedules: {
    id: string;
    title: string;
    start_time: string;
    end_time: string;
    category: string;
  }[];
}
```

- 오늘 일정 시간순 표시 (최대 5건)
- 시간 + 제목 + 카테고리 뱃지
- "전체 보기" 링크 → `/schedule`

### 4.3 데이터 조회

`dashboard/page.tsx`에 추가되는 쿼리:

```typescript
// v0.3D: 스크럼 상태
supabase
  .from("daily_scrums")
  .select("status, scrum_items(count)")
  .eq("user_id", user.id)
  .eq("scrum_date", today)
  .maybeSingle(),

// v0.3D: 오늘 마감 태스크
supabase
  .from("tasks")
  .select("id, title, status, priority, due_date")
  .eq("assignee_id", user.id)
  .eq("due_date", today)
  .neq("status", "completed")
  .order("priority")
  .limit(5),

// v0.3D: 활성 프로젝트 진행률
supabase
  .from("projects")
  .select("id, name, progress, status")
  .in("status", ["planning", "in_progress"])
  .order("updated_at", { ascending: false })
  .limit(4),

// v0.3D: 오늘 일정
supabase
  .from("schedules")
  .select("id, title, start_time, end_time, category")
  .eq("schedule_date", today)
  .order("start_time")
  .limit(5),
```

### 4.4 수정 파일 목록 (Agent-2)

| 파일 | 변경 |
|------|------|
| `src/components/dashboard/dashboard-scrum-widget.tsx` | **신규** |
| `src/components/dashboard/dashboard-tasks-widget.tsx` | **신규** |
| `src/components/dashboard/dashboard-projects-widget.tsx` | **신규** |
| `src/components/dashboard/dashboard-schedule-widget.tsx` | **신규** |
| `src/app/(dashboard)/dashboard/page.tsx` | 쿼리 4개 추가 + 위젯 Row 삽입 |

---

## 5. D3: 사이드바 접이식 3그룹 + 글로벌 검색 확장

### 5.1 사이드바 그룹 구조

**수정 파일**: `src/lib/constants.ts`

```typescript
export interface MenuGroup {
  label: string;
  items: MenuItem[];
}

export const MENU_GROUPS: MenuGroup[] = [
  {
    label: "핵심",
    items: [
      { title: "대시보드", url: "/dashboard", icon: LayoutDashboard, minRole: "normal" },
      { title: "데일리 스크럼", url: "/scrum", icon: Sunrise, minRole: "normal" },
      { title: "TASK", url: "/tasks", icon: CheckSquare, minRole: "normal" },
      { title: "프로젝트", url: "/projects", icon: FolderKanban, minRole: "normal" },
      { title: "일정", url: "/schedule", icon: CalendarDays, minRole: "normal" },
    ],
  },
  {
    label: "지원",
    items: [
      { title: "회고", url: "/retrospective", icon: MessageSquareText, minRole: "normal", children: [...] },
      { title: "회의록", url: "/meetings", icon: FileText, minRole: "normal", children: [...] },
      { title: "입금/결제", url: "/payments", icon: CreditCard, minRole: "normal", children: [...] },
      { title: "견적서", url: "/estimates", icon: Calculator, minRole: "normal", children: [...] },
      { title: "클라이언트 예측", url: "/predict", icon: Brain, minRole: "manager" },
    ],
  },
  {
    label: "설정",
    items: [
      { title: "설정", url: "/settings", icon: Settings, minRole: "boss" },
    ],
  },
];

export function getFilteredMenuGroups(role: UserRole): MenuGroup[] {
  return MENU_GROUPS.map(group => ({
    ...group,
    items: group.items.filter(item => hasMinimumRole(role, item.minRole)),
  })).filter(group => group.items.length > 0);
}
```

### 5.2 사이드바 렌더링 변경

**수정 파일**: `src/components/layout/app-sidebar.tsx`

```
변경 전: 단일 SidebarGroup → menuItems.map(...)
변경 후: getFilteredMenuGroups().map(group =>
           <SidebarGroup key={group.label}>
             <SidebarGroupLabel>{group.label}</SidebarGroupLabel>
             <Collapsible defaultOpen={true}>
               <SidebarGroupContent>
                 {group.items.map(item => ...기존 로직 동일...)}
               </SidebarGroupContent>
             </Collapsible>
           </SidebarGroup>
         )
```

### 5.3 글로벌 검색 확장

**수정 파일**: `src/components/layout/search-bar.tsx`, `src/app/api/search/route.ts`

현재 검색 대상: 견적서, 회의록, 입금/결제, 회고 (4종)
추가 검색 대상: **프로젝트, 태스크, 일정, 팀원** (4종)

```typescript
// search-bar.tsx — SearchResults 인터페이스 확장
interface SearchResults {
  estimates: SearchResult[];
  meetings: SearchResult[];
  payments: SearchResult[];
  retrospectives: SearchResult[];
  // v0.3D 추가
  projects: SearchResult[];
  tasks: SearchResult[];
  schedules: SearchResult[];
  users: SearchResult[];
}

// TYPE_CONFIG 확장
"프로젝트": { label: "프로젝트", variant: "default" },
"태스크": { label: "태스크", variant: "secondary" },
"일정": { label: "일정", variant: "outline" },
"팀원": { label: "팀원", variant: "default" },
```

**API Route 수정**: `src/app/api/search/route.ts`

```typescript
// 프로젝트 검색 (권한 적용 — RLS가 자동 필터)
const { data: projects } = await supabase
  .from("projects")
  .select("id, name, status")
  .ilike("name", `%${q}%`)
  .limit(5);

// 태스크 검색
const { data: tasks } = await supabase
  .from("tasks")
  .select("id, title, status")
  .ilike("title", `%${q}%`)
  .limit(5);

// 일정 검색
const { data: schedules } = await supabase
  .from("schedules")
  .select("id, title, schedule_date")
  .ilike("title", `%${q}%`)
  .limit(5);

// 팀원 검색 (관리자 이상만)
const { data: users } = await supabase
  .from("users")
  .select("id, name, email, role")
  .or(`name.ilike.%${q}%,email.ilike.%${q}%`)
  .limit(5);
```

### 5.4 수정 파일 목록 (Agent-1)

| 파일 | 변경 |
|------|------|
| `src/lib/constants.ts` | `MENU_GROUPS` 구조 변경, `getFilteredMenuGroups()` 추가 |
| `src/components/layout/app-sidebar.tsx` | 3그룹 렌더링 |
| `src/components/layout/search-bar.tsx` | 8종 검색 결과 |
| `src/app/api/search/route.ts` | 프로젝트/태스크/일정/팀원 쿼리 추가 |

---

## 6. D4: 인앱 알림 시스템 확장

### 6.1 기존 인프라 (변경 불필요)

- `notifications` 테이블 ✅
- `NotificationBell` 컴포넌트 (벨 + 드롭다운 + 읽음/안읽음 + 30초 폴링) ✅
- `createNotification()` 유틸 ✅
- `checkDeadlineNotifications()` ✅

### 6.2 알림 트리거 함수 추가

**수정 파일**: `src/lib/notifications.ts`

```typescript
// === v0.3D 알림 트리거 추가 ===

/** 태스크 배정 알림 */
export async function notifyTaskAssigned(
  supabase: SupabaseClient,
  taskId: string,
  taskTitle: string,
  assigneeId: string,
  assignedBy: string
) { ... }

/** 태스크 상태 변경 알림 */
export async function notifyTaskStatusChanged(
  supabase: SupabaseClient,
  taskId: string,
  taskTitle: string,
  newStatus: string,
  projectId: string | null
) { ... }

/** 프로젝트 상태 변경 알림 */
export async function notifyProjectStatusChanged(
  supabase: SupabaseClient,
  projectId: string,
  projectName: string,
  newStatus: string
) { ... }

/** 킥오프 완료 알림 */
export async function notifyKickoffCompleted(
  supabase: SupabaseClient,
  projectId: string,
  projectName: string
) { ... }

/** 코멘트 멘션 알림 */
export async function notifyMention(
  supabase: SupabaseClient,
  mentionedUserId: string,
  mentionerName: string,
  context: string,
  link: string
) { ... }

/** 의존성 블로커 알림 */
export async function notifyDependencyBlocked(
  supabase: SupabaseClient,
  blockedTaskId: string,
  blockedTaskTitle: string,
  blockerTaskTitle: string,
  assigneeId: string,
  pmId: string | null
) { ... }

/** 스크럼 리마인드 알림 (스케줄러용 — 향후 cron 연동) */
export async function notifyScrumReminder(
  supabase: SupabaseClient,
  userId: string
) { ... }
```

### 6.3 트리거 삽입 위치

| 알림 | 트리거 Server Action | 함수 호출 |
|------|---------------------|----------|
| 태스크 배정 | `src/actions/task.ts` → `updateTask()` | `notifyTaskAssigned()` |
| 태스크 상태 변경 | `src/actions/task.ts` → `updateTaskStatus()` | `notifyTaskStatusChanged()` |
| 프로젝트 상태 변경 | `src/actions/project.ts` → `updateProjectStatus()` | `notifyProjectStatusChanged()` |
| 킥오프 완료 | `src/actions/kickoff.ts` → `completeKickoff()` | `notifyKickoffCompleted()` |
| 의존성 블로커 | `src/actions/task.ts` → `updateTaskStatus('blocked')` | `notifyDependencyBlocked()` |

### 6.4 수정 파일 목록 (Agent-2)

| 파일 | 변경 |
|------|------|
| `src/lib/notifications.ts` | 7개 트리거 함수 추가 |
| `src/actions/task.ts` | 상태변경/배정 시 알림 호출 삽입 |
| `src/actions/project.ts` | 프로젝트 상태 변경 시 알림 호출 삽입 |
| `src/actions/kickoff.ts` | 킥오프 완료 시 알림 호출 삽입 |

---

## 7. D5: 회의록 ↔ 태스크 양방향 연동

### 7.1 DB 스키마

```sql
-- action_items 테이블에 태스크 연결 컬럼 추가
ALTER TABLE action_items ADD COLUMN IF NOT EXISTS linked_task_id uuid REFERENCES tasks(id) ON DELETE SET NULL;
CREATE INDEX IF NOT EXISTS idx_action_items_task ON action_items(linked_task_id);
```

### 7.2 양방향 동기화 로직

**신규 파일**: `src/actions/meeting-task-sync.ts`

```typescript
"use server";

/** 액션아이템 → 태스크 자동 생성 */
export async function createTaskFromActionItem(
  actionItemId: string,
  projectId: string | null
): Promise<{ taskId: string } | { error: string }> {
  // 1. action_item 조회
  // 2. tasks 테이블에 INSERT (source: "meeting", title, assignee, due_date)
  // 3. action_items.linked_task_id 업데이트
  // 4. 알림 트리거
}

/** 태스크 상태 변경 → 액션아이템 동기화 */
export async function syncTaskStatusToActionItem(
  taskId: string,
  newStatus: string
): Promise<void> {
  // 1. action_items에서 linked_task_id = taskId 조회
  // 2. 매칭되는 action_item 상태 업데이트
  // ⚠️ sync_source 플래그로 무한 루프 방지
}

/** 액션아이템 상태 변경 → 태스크 동기화 */
export async function syncActionItemStatusToTask(
  actionItemId: string,
  newStatus: string
): Promise<void> {
  // 1. action_items에서 linked_task_id 조회
  // 2. 연결된 task 상태 업데이트
  // ⚠️ sync_source 플래그로 무한 루프 방지
}
```

### 7.3 무한 루프 방지

```typescript
// 동기화 시 _sync_source 메타 플래그를 사용
// 태스크 → 액션아이템: _sync_source = "task"
// 액션아이템 → 태스크: _sync_source = "action_item"
// 동기화 함수 내에서 동일 source이면 스킵
```

### 7.4 태스크 상세 출처 표시

**수정 파일**: `src/components/tasks/task-detail-panel.tsx`

```
기존 태스크 상세에 추가:
─────────────────────
출처: 📋 회의록 "2주차 스프린트 회의" 에서 생성
      → 클릭하면 /meetings/{meetingId} 이동
─────────────────────
```

### 7.5 수정 파일 목록 (Agent-3)

| 파일 | 변경 |
|------|------|
| `supabase/migrations/021_cross_module.sql` | **신규** — linked_task_id + project_id 추가 |
| `src/actions/meeting-task-sync.ts` | **신규** — 양방향 동기화 3함수 |
| `src/components/tasks/task-detail-panel.tsx` | 출처 링크 표시 추가 |
| `src/actions/task.ts` | 상태 변경 시 `syncTaskStatusToActionItem()` 호출 |

---

## 8. D6: 회고 ↔ 프로젝트 연동

### 8.1 DB 스키마

```sql
-- retrospectives 테이블에 프로젝트 연결 컬럼 추가
ALTER TABLE retrospectives ADD COLUMN IF NOT EXISTS project_id uuid REFERENCES projects(id) ON DELETE SET NULL;
CREATE INDEX IF NOT EXISTS idx_retrospectives_project ON retrospectives(project_id);
```

### 8.2 프로젝트 완료 → 회고 유도

**수정 파일**: `src/actions/project.ts`

```typescript
// completeProject() 함수 내에 추가:
// 프로젝트 유형 확인 → 프로젝트/미니프로젝트면 회고 유도 반환
export async function completeProject(projectId: string): Promise<{
  success: boolean;
  suggestRetrospective?: boolean;  // v0.3D 추가
  projectType?: string;
}> { ... }
```

**신규 파일**: `src/components/projects/project-completion-dialog.tsx`

```typescript
// 프로젝트 완료 시 다이얼로그:
// - "프로젝트가 완료되었습니다!"
// - 프로젝트 유형이 project/mini이면:
//   "📝 회고를 작성하시겠습니까?" [작성하기] [나중에]
// - 셀프 프로젝트: 결과보고로 대체 안내
```

### 8.3 수정 파일 목록 (Agent-3)

| 파일 | 변경 |
|------|------|
| `supabase/migrations/021_cross_module.sql` | project_id 컬럼 추가 (D5와 통합) |
| `src/actions/project.ts` | `completeProject()` 회고 유도 반환값 추가 |
| `src/components/projects/project-completion-dialog.tsx` | **신규** — 회고 유도 다이얼로그 |

---

## 9. 병렬 작업 파일 분리 매트릭스

> 에이전트 간 파일 충돌 방지를 위한 엄격한 분리

| 파일 | Agent-1 | Agent-2 | Agent-3 | 리더 |
|------|:-------:|:-------:|:-------:|:----:|
| `migrations/020_role_permissions.sql` | ✍ | | | |
| `migrations/021_cross_module.sql` | | | ✍ | |
| `src/lib/auth-guard.ts` | ✍ | | | |
| `src/lib/constants.ts` | ✍ | | | |
| `src/lib/notifications.ts` | | ✍ | | |
| `src/components/layout/app-sidebar.tsx` | ✍ | | | |
| `src/components/layout/search-bar.tsx` | ✍ | | | |
| `src/app/api/search/route.ts` | ✍ | | | |
| `src/app/(dashboard)/dashboard/page.tsx` | | ✍ | | |
| `src/components/dashboard/*-widget.tsx` (4종) | | ✍ | | |
| `src/actions/notification.ts` | | ✍ | | |
| `src/actions/meeting-task-sync.ts` | | | ✍ | |
| `src/components/tasks/task-detail-panel.tsx` | | | ✍ | |
| `src/components/projects/project-completion-dialog.tsx` | | | ✍ | |
| `src/actions/project.ts` | ✍(권한) | ✍(알림) | ✍(회고) | 🔀 |
| `src/actions/task.ts` | ✍(권한) | ✍(알림) | ✍(동기화) | 🔀 |
| `src/actions/kickoff.ts` | | ✍(알림) | | |

> 🔀 = 리더가 머지 시 충돌 해결. `project.ts`와 `task.ts`는 3개 에이전트가 각기 다른 함수를 수정하므로 충돌 최소화. 리더가 최종 통합.

---

## 10. 구현 순서 (에이전트별)

### Agent-1 (sonnet) — 권한 + 사이드바 + 검색

| Step | 작업 | 예상 |
|------|------|------|
| 1 | `020_role_permissions.sql` 작성 + 적용 | 1h |
| 2 | `auth-guard.ts` 생성 | 0.5h |
| 3 | `project.ts`, `task.ts`에 권한 체크 삽입 | 1.5h |
| 4 | 프로젝트 상세 UI 조건부 렌더링 | 1h |
| 5 | `constants.ts` → `MENU_GROUPS` 구조 변경 | 1h |
| 6 | `app-sidebar.tsx` → 3그룹 렌더링 | 1.5h |
| 7 | `search-bar.tsx` + `api/search/route.ts` 확장 | 1.5h |
| 8 | 빌드 검증 | 0.5h |
| **합계** | | **8.5h** |

### Agent-2 (sonnet) — 대시보드 위젯 + 알림

| Step | 작업 | 예상 |
|------|------|------|
| 1 | `dashboard-scrum-widget.tsx` 생성 | 1h |
| 2 | `dashboard-tasks-widget.tsx` 생성 | 1h |
| 3 | `dashboard-projects-widget.tsx` 생성 | 1h |
| 4 | `dashboard-schedule-widget.tsx` 생성 | 1h |
| 5 | `dashboard/page.tsx` 수정 (쿼리 + 위젯 삽입) | 2h |
| 6 | `notifications.ts` 트리거 7함수 추가 | 2h |
| 7 | `task.ts`, `project.ts`, `kickoff.ts`에 알림 호출 삽입 | 2h |
| 8 | 빌드 검증 | 0.5h |
| **합계** | | **10.5h** |

### Agent-3 (haiku) — 크로스 모듈 연동

| Step | 작업 | 예상 |
|------|------|------|
| 1 | `021_cross_module.sql` 작성 + 적용 | 0.5h |
| 2 | `meeting-task-sync.ts` 생성 (3함수) | 2h |
| 3 | `task-detail-panel.tsx` 출처 링크 추가 | 0.5h |
| 4 | `task.ts` 상태 변경 시 동기화 호출 삽입 | 0.5h |
| 5 | `project.ts` → `completeProject()` 회고 유도 | 0.5h |
| 6 | `project-completion-dialog.tsx` 생성 | 1h |
| 7 | 빌드 검증 | 0.5h |
| **합계** | | **5.5h** |

### 리더 (opus) — 통합 + QA

| Step | 작업 | 예상 |
|------|------|------|
| 1 | DB 마이그레이션 실행 (020, 021) | 0.5h |
| 2 | 에이전트 결과 머지 + 충돌 해결 | 1h |
| 3 | 전체 빌드 검증 (`next build` 0 에러) | 0.5h |
| 4 | 브라우저 QA (4조합 × 전체 시나리오) | 4h |
| 5 | 이슈 수정 반복 (문제 0건까지) | 2h+ |
| **합계** | | **8h+** |

---

## 11. 테스트 계획

### 11.1 테스트 루틴
```
[구현 완료] → [next build 0 에러] → [브라우저 4조합 테스트]
→ [이슈?] → YES: 수정 후 재테스트 / NO: PASS
```

### 11.2 4조합 테스트

| 조합 | 해상도 | 테마 |
|------|--------|------|
| Desktop Light | 1440x900 | light |
| Desktop Dark | 1440x900 | dark |
| Mobile Light | 375x812 | light |
| Mobile Dark | 375x812 | dark |

### 11.3 테스트 시나리오

| # | 시나리오 | 검증 항목 |
|---|----------|----------|
| 1 | normal 사용자 로그인 | 사이드바 "설정" 안 보임, 본인 프로젝트만 표시 |
| 2 | manager 사용자 로그인 | 담당 프로젝트 + 팀원 스크럼 열람 |
| 3 | super 사용자 로그인 | 전사 현황, 삭제/승인 버튼 표시 |
| 4 | 사이드바 3그룹 | 핵심/지원/설정 그룹 토글, 현재 메뉴 하이라이트 |
| 5 | Cmd+K 검색 | 프로젝트, 태스크, 일정, 팀원 검색 결과 표시 |
| 6 | 대시보드 스크럼 위젯 | 오늘 스크럼 상태 정확 표시 + 바로가기 |
| 7 | 대시보드 태스크 위젯 | 오늘 마감 태스크 표시 + 전체 보기 |
| 8 | 대시보드 프로젝트 위젯 | 진행률 바 정확 표시 |
| 9 | 대시보드 일정 위젯 | 오늘 일정 시간순 표시 |
| 10 | 알림: 태스크 배정 | 담당자 변경 → 알림 생성 → 벨 뱃지 |
| 11 | 알림: 프로젝트 완료 | 프로젝트 완료 → 멤버 전원 알림 |
| 12 | 회의록 → 태스크 | 액션아이템 생성 → 태스크 자동 생성 확인 |
| 13 | 태스크 → 액션아이템 | 태스크 완료 → 액션아이템 상태 동기화 |
| 14 | 태스크 상세 출처 | "출처: 회의록 #N" 링크 표시 |
| 15 | 프로젝트 완료 → 회고 | 완료 → "회고 작성하시겠습니까?" 다이얼로그 |
| 16 | 회귀: v0.3A | 프로젝트/태스크/의존성맵/AI리뷰 정상 |
| 17 | 회귀: v0.3B | 킥오프/일정 정상 |
| 18 | 회귀: v0.3C | 데일리 스크럼 전체 플로우 정상 |
| 19 | 프로젝트 삭제 | PM 본인 생성분 삭제 가능, normal 삭제 불가 |
| 20 | 모바일 반응형 | 사이드바 접힘, 위젯 스택, 검색 정상 |

---

## 12. 보안 고려사항

- [x] RLS 정책으로 DB 레벨 접근 제어 (설계 완료)
- [ ] Server Action에서 `requireRole()` 이중 체크
- [ ] 검색 API에서 RLS 자동 필터링 (Supabase 기본)
- [ ] 소프트 삭제 시 cascade 검증 (하위 태스크/킥오프 등)
- [ ] 양방향 동기화 무한 루프 방지 (sync_source 플래그)
- [ ] 알림 대량 생성 시 rate limiting (프로젝트 멤버 수 제한)

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 0.1 | 2026-03-07 | Initial draft | Claude (PDCA) |
