# Design: v0.3B — 프로젝트 킥오프 + 일정 캘린더

> **Feature**: 0.3B
> **Created**: 2026-03-06
> **Plan Reference**: `docs/01-plan/features/0.3B.plan.md`
> **Status**: Draft

---

## 1. DB 마이그레이션

**파일**: `supabase/migrations/018_kickoff_and_schedules.sql`

### 1.1 project_kickoffs 테이블

```sql
CREATE TABLE project_kickoffs (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  project_id uuid NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
  objective text,
  scope text,
  constraints text,
  success_criteria text,
  kickoff_date date,
  meeting_notes text,
  meeting_attendees uuid[] DEFAULT '{}',
  meeting_decisions jsonb DEFAULT '[]',
  status text NOT NULL DEFAULT 'draft' CHECK (status IN ('draft', 'in_progress', 'completed')),
  completed_at timestamptz,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now(),
  UNIQUE(project_id)
);

CREATE INDEX idx_kickoffs_project ON project_kickoffs(project_id);

ALTER TABLE project_kickoffs ENABLE ROW LEVEL SECURITY;

CREATE POLICY "kickoffs_select" ON project_kickoffs
  FOR SELECT TO authenticated USING (true);
CREATE POLICY "kickoffs_insert" ON project_kickoffs
  FOR INSERT TO authenticated WITH CHECK (true);
CREATE POLICY "kickoffs_update" ON project_kickoffs
  FOR UPDATE TO authenticated USING (true);
CREATE POLICY "kickoffs_delete" ON project_kickoffs
  FOR DELETE TO authenticated
  USING (EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role IN ('super', 'boss', 'manager')));

CREATE TRIGGER set_kickoffs_updated_at
  BEFORE UPDATE ON project_kickoffs
  FOR EACH ROW
  EXECUTE FUNCTION update_updated_at();
```

### 1.2 kickoff_checklist_items 테이블

```sql
CREATE TABLE kickoff_checklist_items (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  kickoff_id uuid NOT NULL REFERENCES project_kickoffs(id) ON DELETE CASCADE,
  title text NOT NULL,
  description text,
  assignee_id uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  is_completed boolean DEFAULT false,
  due_date date,
  sort_order integer DEFAULT 0,
  created_at timestamptz DEFAULT now()
);

CREATE INDEX idx_checklist_kickoff ON kickoff_checklist_items(kickoff_id);

ALTER TABLE kickoff_checklist_items ENABLE ROW LEVEL SECURITY;

CREATE POLICY "checklist_select" ON kickoff_checklist_items
  FOR SELECT TO authenticated USING (true);
CREATE POLICY "checklist_insert" ON kickoff_checklist_items
  FOR INSERT TO authenticated WITH CHECK (true);
CREATE POLICY "checklist_update" ON kickoff_checklist_items
  FOR UPDATE TO authenticated USING (true);
CREATE POLICY "checklist_delete" ON kickoff_checklist_items
  FOR DELETE TO authenticated USING (true);
```

### 1.3 schedules 테이블

```sql
CREATE TABLE schedules (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  title text NOT NULL,
  description text,
  category text NOT NULL DEFAULT 'other'
    CHECK (category IN ('vacation', 'salary_review', 'birthday', 'holiday', 'company', 'meeting', 'other')),
  start_date date NOT NULL,
  end_date date,
  is_all_day boolean DEFAULT true,
  start_time time,
  end_time time,
  color text,
  created_by uuid NOT NULL REFERENCES auth.users(id),
  is_public boolean DEFAULT true,
  target_user_id uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  created_at timestamptz DEFAULT now()
);

CREATE INDEX idx_schedules_dates ON schedules(start_date, end_date);
CREATE INDEX idx_schedules_created_by ON schedules(created_by);
CREATE INDEX idx_schedules_category ON schedules(category);

ALTER TABLE schedules ENABLE ROW LEVEL SECURITY;

CREATE POLICY "schedules_select" ON schedules
  FOR SELECT TO authenticated
  USING (is_public = true OR created_by = auth.uid() OR target_user_id = auth.uid());
CREATE POLICY "schedules_insert" ON schedules
  FOR INSERT TO authenticated
  WITH CHECK (auth.uid() = created_by);
CREATE POLICY "schedules_update" ON schedules
  FOR UPDATE TO authenticated
  USING (
    created_by = auth.uid()
    OR EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role IN ('super', 'boss', 'manager'))
  );
CREATE POLICY "schedules_delete" ON schedules
  FOR DELETE TO authenticated
  USING (
    created_by = auth.uid()
    OR EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role IN ('super', 'boss', 'manager'))
  );
```

---

## 2. TypeScript 타입 정의

**파일**: `src/types/kickoff.types.ts`

```typescript
export interface ProjectKickoff {
  id: string;
  project_id: string;
  objective: string | null;
  scope: string | null;
  constraints: string | null;
  success_criteria: string | null;
  kickoff_date: string | null;
  meeting_notes: string | null;
  meeting_attendees: string[];
  meeting_decisions: MeetingDecision[];
  status: KickoffStatus;
  completed_at: string | null;
  created_at: string;
  updated_at: string;
}

export type KickoffStatus = "draft" | "in_progress" | "completed";

export interface MeetingDecision {
  title: string;
  content: string;
}

export interface KickoffChecklistItem {
  id: string;
  kickoff_id: string;
  title: string;
  description: string | null;
  assignee_id: string | null;
  is_completed: boolean;
  due_date: string | null;
  sort_order: number;
  created_at: string;
  // joined
  assignee?: { name: string } | null;
}
```

**파일**: `src/types/schedule.types.ts`

```typescript
export type ScheduleCategory =
  | "vacation"
  | "salary_review"
  | "birthday"
  | "holiday"
  | "company"
  | "meeting"
  | "other";

export interface Schedule {
  id: string;
  title: string;
  description: string | null;
  category: ScheduleCategory;
  start_date: string;
  end_date: string | null;
  is_all_day: boolean;
  start_time: string | null;
  end_time: string | null;
  color: string | null;
  created_by: string;
  is_public: boolean;
  target_user_id: string | null;
  created_at: string;
  // joined
  creator?: { name: string } | null;
}

export const SCHEDULE_CATEGORY_CONFIG: Record<
  ScheduleCategory,
  { label: string; color: string; icon: string }
> = {
  vacation: { label: "방학", color: "#10B981", icon: "Palmtree" },
  salary_review: { label: "연봉협상", color: "#F97316", icon: "HandCoins" },
  birthday: { label: "생일", color: "#EC4899", icon: "Cake" },
  holiday: { label: "공휴일", color: "#EF4444", icon: "CalendarOff" },
  company: { label: "회사 일정", color: "#FEEB00", icon: "Building2" },
  meeting: { label: "회의", color: "#3B82F6", icon: "Users" },
  other: { label: "기타", color: "#6B7280", icon: "Calendar" },
};
```

---

## 3. Server Actions

### 3.1 킥오프 Server Actions

**파일**: `src/actions/kickoff.ts`

```typescript
"use server";

// ─── 킥오프 CRUD ───

// createKickoff(projectId: string): ProjectKickoff
// - 프로젝트 생성 시 자동 호출 (project_type === "project" 일 때)
// - status: "draft"로 생성
// - 기본 체크리스트 5항목 자동 삽입
// - revalidatePath: /projects/[id], /projects/[id]/kickoff

// updateKickoff(id: string, data: Partial<ProjectKickoff>): ProjectKickoff
// - objective, scope, constraints, success_criteria, kickoff_date, meeting_notes, meeting_attendees, meeting_decisions 수정
// - status가 "draft"이면 자동으로 "in_progress"로 변경
// - revalidatePath: /projects/[id]/kickoff

// completeKickoff(id: string): ProjectKickoff
// - 전제조건: 모든 checklist_items.is_completed === true
// - status → "completed", completed_at 설정
// - revalidatePath: /projects/[id], /projects/[id]/kickoff, /projects/[id]/tasks

// ─── 체크리스트 CRUD ───

// addChecklistItem(kickoffId: string, data: { title, description?, assignee_id?, due_date? }): KickoffChecklistItem
// - sort_order: 현재 max + 1
// - revalidatePath: /projects/[id]/kickoff

// toggleChecklistItem(id: string): KickoffChecklistItem
// - is_completed 토글
// - revalidatePath: /projects/[id]/kickoff

// reorderChecklist(kickoffId: string, itemIds: string[]): void
// - itemIds 배열 순서대로 sort_order 재설정 (0, 1, 2, ...)
// - revalidatePath: /projects/[id]/kickoff

// deleteChecklistItem(id: string): void
// - revalidatePath: /projects/[id]/kickoff

// updateChecklistItem(id: string, data: Partial<{ title, description, assignee_id, due_date }>): KickoffChecklistItem
// - revalidatePath: /projects/[id]/kickoff
```

#### 기본 체크리스트 자동 삽입

```typescript
const DEFAULT_CHECKLIST_ITEMS = [
  { title: "프로젝트 목표 확정", description: "프로젝트 목표와 기대 효과를 명확히 정의합니다." },
  { title: "팀원 배정 완료", description: "역할별 담당자를 배정하고 R&R을 확인합니다." },
  { title: "마일스톤 설정 완료", description: "주요 일정과 마일스톤을 설정합니다." },
  { title: "레퍼런스/자료 수집", description: "필요한 레퍼런스와 자료를 수집합니다." },
  { title: "위험 요소 식별", description: "잠재적 리스크와 대응 방안을 정리합니다." },
];
```

### 3.2 스케줄 Server Actions

**파일**: `src/actions/schedule.ts`

```typescript
"use server";

// createSchedule(data: Omit<Schedule, "id" | "created_by" | "created_at">): Schedule
// - created_by: 현재 사용자
// - color: category에 따라 SCHEDULE_CATEGORY_CONFIG에서 자동 설정 (사용자 미지정 시)
// - revalidatePath: /schedule

// updateSchedule(id: string, data: Partial<Schedule>): Schedule
// - 권한 검증: created_by === 현재 사용자 || role in ('super', 'boss', 'manager')
// - revalidatePath: /schedule

// deleteSchedule(id: string): void
// - 권한 검증: 동일
// - revalidatePath: /schedule
```

---

## 4. 컴포넌트 상세 설계

### 4.1 킥오프 컴포넌트 (7종)

#### `src/components/kickoff/kickoff-overview.tsx`

```
Props: { kickoff: ProjectKickoff; projectId: string; canEdit: boolean }
역할: 목표/범위/제약/성공기준 4개 필드를 카드형 폼으로 표시
편집: 인라인 편집 (Textarea + 저장 버튼) — canEdit=true일 때만
상태: status !== "completed"일 때만 편집 가능
UI: Card 4장 (2x2 grid on desktop, 1 col on mobile)
```

#### `src/components/kickoff/kickoff-team.tsx`

```
Props: { members: ProjectMember[]; projectId: string }
역할: project_members 테이블에서 팀원 목록 표시 (읽기 전용)
UI: 멤버 카드 그리드 (아바타 + 이름 + 역할 뱃지)
링크: "멤버 관리" 버튼 → /projects/[id]/settings 이동
```

#### `src/components/kickoff/kickoff-milestones.tsx`

```
Props: { milestones: ProjectMilestone[]; projectId: string }
역할: project_milestones 테이블에서 마일스톤 타임라인 표시 (읽기 전용)
UI: 기존 ProjectMilestoneTimeline 컴포넌트 재사용
링크: "마일스톤 관리" 버튼 → /projects/[id]/settings 이동
```

#### `src/components/kickoff/kickoff-checklist.tsx`

```
Props: { kickoffId: string; items: KickoffChecklistItem[]; canEdit: boolean; users: { id: string; name: string }[] }
역할: 체크리스트 CRUD + 드래그 정렬
UI:
  - 각 항목: 체크박스 + 제목 + 담당자 뱃지 + 기한 + 삭제 버튼
  - 드래그 핸들 (GripVertical 아이콘)
  - "항목 추가" 버튼 → 인라인 폼 (제목 + 담당자 셀렉트 + 기한 DatePicker)
  - 완료 시 is_completed → 취소선 스타일
기술:
  - @dnd-kit/core + @dnd-kit/sortable 사용 (기존 Kanban과 동일 패턴)
  - 드래그 끝나면 reorderChecklist Server Action 호출
  - 체크 토글: toggleChecklistItem
  - 삭제: deleteChecklistItem
상태: status === "completed"이면 편집 불가 (읽기 전용)
```

#### `src/components/kickoff/kickoff-meeting-notes.tsx`

```
Props: { kickoff: ProjectKickoff; projectId: string; users: { id: string; name: string }[]; canEdit: boolean }
역할: 킥오프 미팅 기록 (참석자 + 결정사항 + 노트)
UI:
  - 참석자: MultiSelect (users 목록에서 선택)
  - 결정사항: 동적 리스트 [{title, content}] + 추가/삭제
  - 노트: Textarea (리치 텍스트는 v1에서 불필요, 플레인 텍스트)
저장: updateKickoff 호출 (meeting_attendees, meeting_decisions, meeting_notes)
```

#### `src/components/kickoff/kickoff-progress.tsx`

```
Props: { total: number; completed: number; status: KickoffStatus }
역할: 킥오프 완료율 프로그레스 바
UI:
  - 프로그레스 바 (completed/total * 100%)
  - 텍스트: "3/5 완료 (60%)"
  - status에 따른 뱃지: draft=초안, in_progress=진행중, completed=완료
```

#### `src/components/kickoff/kickoff-complete-button.tsx`

```
Props: { kickoffId: string; allCompleted: boolean; status: KickoffStatus; projectId: string }
역할: 킥오프 완료 버튼
UI:
  - allCompleted=false → 버튼 disabled + 툴팁 "모든 체크리스트를 완료해주세요"
  - allCompleted=true → 버튼 활성화 "킥오프 완료"
  - status=completed → "킥오프 완료됨" 뱃지 (버튼 대신)
동작: completeKickoff Server Action 호출 → 성공 시 toast + revalidate
```

### 4.2 스케줄 컴포넌트 (5종)

#### `src/components/schedule/schedule-calendar.tsx`

```
Props: { events: Schedule[]; currentView: "month" | "week" | "day"; onDateClick: (date) => void; onEventClick: (event) => void }
역할: FullCalendar 래퍼
기술:
  - "use client" (FullCalendar는 클라이언트 컴포넌트)
  - dynamic import: @fullcalendar/react, @fullcalendar/daygrid, @fullcalendar/timegrid, @fullcalendar/interaction
  - events를 FullCalendar EventInput 형식으로 변환
  - 다크모드: CSS 변수 기반 FullCalendar 테마 오버라이드
  - 반응형: 모바일에서 headerToolbar 축소
변환 로직:
  Schedule → EventInput:
    title: schedule.title
    start: schedule.is_all_day ? schedule.start_date : `${schedule.start_date}T${schedule.start_time}`
    end: schedule.end_date ?? schedule.start_date (+ end_time if not all_day)
    backgroundColor: schedule.color ?? SCHEDULE_CATEGORY_CONFIG[schedule.category].color
    allDay: schedule.is_all_day
    extendedProps: { schedule }
```

#### `src/components/schedule/schedule-event-dialog.tsx`

```
Props: { open: boolean; onOpenChange; mode: "create" | "edit"; schedule?: Schedule; selectedDate?: string; onSubmit: (data) => void }
역할: 일정 생성/수정 다이얼로그
UI (shadcn Dialog):
  - 제목: Input
  - 설명: Textarea
  - 카테고리: Select (7종)
  - 종일 여부: Switch
  - 시작일: DatePicker (필수)
  - 종료일: DatePicker (선택)
  - 시작시간/종료시간: Input type="time" (is_all_day=false일 때만)
  - 공개 여부: Switch
  - 대상 사용자: Select (is_public=false일 때만)
유효성:
  - title 필수
  - start_date 필수
  - end_date가 있으면 start_date 이후여야 함
  - is_all_day=false이면 start_time 필수
```

#### `src/components/schedule/schedule-sidebar.tsx`

```
Props: { categories: ScheduleCategory[]; activeCategories: Set<ScheduleCategory>; onToggle: (cat) => void; onMyScheduleToggle: () => void; isMyScheduleOnly: boolean }
역할: 카테고리 필터 + "내 일정만" 토글
UI:
  - 카테고리 목록: 색상 도트 + 체크박스 + 라벨
  - "내 일정만 보기" 토글 스위치
  - 모바일: Sheet로 표시 (햄버거 메뉴)
```

#### `src/components/schedule/schedule-event-card.tsx`

```
Props: { schedule: Schedule; canEdit: boolean; onEdit: () => void; onDelete: () => void }
역할: 일정 상세 팝오버 (캘린더 이벤트 클릭 시)
UI (Popover):
  - 카테고리 뱃지 (색상 + 라벨)
  - 제목, 설명
  - 날짜/시간
  - 작성자
  - canEdit=true → 수정/삭제 버튼
```

#### `src/components/schedule/schedule-view-toggle.tsx`

```
Props: { currentView: "month" | "week" | "day"; onViewChange: (view) => void }
역할: 월/주/일 뷰 전환
UI: ToggleGroup (shadcn) — Calendar / CalendarDays / Clock 아이콘
```

---

## 5. 페이지 설계

### 5.1 킥오프 페이지

**파일**: `src/app/(dashboard)/projects/[id]/kickoff/page.tsx`

```
Server Component:
1. requireAuth()로 인증 확인
2. project 조회 (id, name, project_type)
3. project_type이 "project"가 아니면 → redirect(/projects/[id])
4. project_kickoffs 조회 (project_id 기준, 없으면 자동 생성)
5. kickoff_checklist_items 조회 (kickoff_id, sort_order 정렬, assignee join)
6. project_members 조회 (user join)
7. project_milestones 조회 (target_date 정렬)
8. users 목록 조회 (체크리스트 담당자 선택용)

레이아웃:
┌──────────────────────────────────────────┐
│  ← 프로젝트명 > 킥오프                      │
│  [킥오프 진행률 프로그레스 바]               │
├──────────────────────────────────────────┤
│  ┌─── 프로젝트 개요 ────┐ ┌─ 팀 구성 ──┐  │
│  │ 목표 | 범위          │ │ 멤버 카드   │  │
│  │ 제약 | 성공기준      │ │            │  │
│  └─────────────────────┘ └────────────┘  │
├──────────────────────────────────────────┤
│  [마일스톤 타임라인]                        │
├──────────────────────────────────────────┤
│  [체크리스트]                               │
│  ☐ 프로젝트 목표 확정     @김성현  D-3     │
│  ☑ 팀원 배정 완료         @이준호          │
│  ☐ 마일스톤 설정 완료     -       D-5     │
│  ☐ 레퍼런스/자료 수집     @박지민          │
│  ☐ 위험 요소 식별        -                │
│  [+ 항목 추가]                             │
├──────────────────────────────────────────┤
│  [미팅 기록]                               │
│  참석자: 김성현, 이준호, 박지민             │
│  결정사항: ...                             │
│  노트: ...                                │
├──────────────────────────────────────────┤
│  [킥오프 완료 버튼]                         │
└──────────────────────────────────────────┘
```

### 5.2 스케줄 페이지

**파일**: `src/app/(dashboard)/schedule/page.tsx`

```
Server Component:
1. requireAuth()로 인증 확인
2. searchParams에서 view 파싱 (default: "month")
3. schedules 조회 (is_public=true OR created_by=me, creator join)
4. 현재 사용자 role 조회 (수정 권한 판단용)

레이아웃:
┌──────────────────────────────────────────────────┐
│  일정                    [월|주|일] [+ 일정 추가]   │
├──────────┬───────────────────────────────────────┤
│ 사이드바  │                                       │
│          │         FullCalendar                  │
│ □ 방학   │                                       │
│ □ 연봉   │         (월간/주간/일간 뷰)             │
│ □ 생일   │                                       │
│ □ 공휴일 │                                       │
│ □ 회사   │                                       │
│ □ 회의   │                                       │
│ □ 기타   │                                       │
│          │                                       │
│ ─────── │                                       │
│ □내일정만│                                       │
├──────────┴───────────────────────────────────────┤
│ 모바일: 사이드바 → Sheet, 캘린더 전체 폭           │
└──────────────────────────────────────────────────┘
```

**파일**: `src/app/(dashboard)/schedule/loading.tsx`

```
Skeleton: 사이드바 + 캘린더 영역 펄스 애니메이션
```

---

## 6. 기존 파일 수정

### 6.1 프로젝트 허브 탭에 킥오프 추가

**파일**: `src/components/projects/project-hub-tabs.tsx`

**변경**:
- `getLinkTabs()` 함수에 킥오프 탭 추가 (tasks 뒤, dependency-map 앞)
- 조건: project.project_type === "project"일 때만 표시

```typescript
// 추가할 탭
{
  key: "kickoff",
  label: "킥오프",
  icon: Rocket,          // lucide-react
  path: `/projects/${projectId}/kickoff`,
  description: "프로젝트 킥오프 체크리스트와 미팅 기록을 관리합니다.",
}
```

- `ProjectHubTabsProps.project`에 `project_type` 이미 존재 → 필터링만 추가

### 6.2 태스크 생성 전제조건

**파일**: `src/actions/task.ts` — `createTask` 함수

**변경**:
- `data.project_id`가 있을 때, 해당 프로젝트의 `project_type`을 확인
- `project_type === "project"`이면 `project_kickoffs` 테이블에서 `status === "completed"` 확인
- 킥오프 미완료 시 에러: `"킥오프를 먼저 완료해주세요. 프로젝트 킥오프 탭에서 체크리스트를 완료한 후 태스크를 생성할 수 있습니다."`

```typescript
// createTask 함수 내부 (project_id 존재 시)
if (data.project_id) {
  const { data: project } = await supabase
    .from("projects")
    .select("project_type")
    .eq("id", data.project_id)
    .single();

  if (project?.project_type === "project") {
    const { data: kickoff } = await supabase
      .from("project_kickoffs")
      .select("status")
      .eq("project_id", data.project_id)
      .single();

    if (!kickoff || kickoff.status !== "completed") {
      throw new Error("킥오프를 먼저 완료해주세요. 프로젝트 킥오프 탭에서 체크리스트를 완료한 후 태스크를 생성할 수 있습니다.");
    }
  }
}
```

### 6.3 사이드바 메뉴에 "일정" 추가

**파일**: `src/lib/constants.ts`

**변경**:
- `MENU_ITEMS` 배열에 "일정" 항목 추가 (TASK 뒤)
- import: `CalendarDays` from lucide-react

```typescript
{
  title: "일정",
  url: "/schedule",
  icon: CalendarDays,
  minRole: "normal",
},
```

### 6.4 프로젝트 생성 시 킥오프 자동 생성

**파일**: `src/actions/project.ts` — `createProject` 함수 (또는 해당 함수)

**변경**:
- 프로젝트 생성 후, `project_type === "project"`이면 `createKickoff` 호출
- 기본 체크리스트 5항목 자동 삽입

---

## 7. FullCalendar 다크모드 스타일

**파일**: `src/app/globals.css` (또는 별도 `schedule.css`)

```css
/* FullCalendar 다크모드 오버라이드 */
.dark .fc {
  --fc-border-color: hsl(var(--border));
  --fc-button-bg-color: hsl(var(--primary));
  --fc-button-border-color: hsl(var(--primary));
  --fc-button-hover-bg-color: hsl(var(--primary) / 0.8);
  --fc-button-active-bg-color: hsl(var(--primary) / 0.9);
  --fc-page-bg-color: hsl(var(--background));
  --fc-neutral-bg-color: hsl(var(--muted));
  --fc-today-bg-color: hsl(var(--accent) / 0.3);
  --fc-event-text-color: white;
}

.fc .fc-daygrid-day-number,
.fc .fc-col-header-cell-cushion {
  color: hsl(var(--foreground));
}
```

---

## 8. 구현 순서 (상세)

| # | 작업 | 신규/수정 | 파일 |
|---|------|----------|------|
| 1 | DB 마이그레이션 | 신규 | `supabase/migrations/018_kickoff_and_schedules.sql` |
| 2 | TypeScript 타입 정의 | 신규 | `src/types/kickoff.types.ts`, `src/types/schedule.types.ts` |
| 3 | 킥오프 Server Actions | 신규 | `src/actions/kickoff.ts` |
| 4 | 킥오프 컴포넌트 7종 | 신규 | `src/components/kickoff/*.tsx` (7파일) |
| 5 | 킥오프 페이지 | 신규 | `src/app/(dashboard)/projects/[id]/kickoff/page.tsx` |
| 6 | 프로젝트 허브 탭 수정 | 수정 | `src/components/projects/project-hub-tabs.tsx` |
| 7 | 태스크 생성 전제조건 | 수정 | `src/actions/task.ts` |
| 8 | 프로젝트 생성 시 킥오프 자동생성 | 수정 | `src/actions/project.ts` |
| 9 | 스케줄 Server Actions | 신규 | `src/actions/schedule.ts` |
| 10 | FullCalendar 설치 | - | `npm install` |
| 11 | 스케줄 컴포넌트 5종 | 신규 | `src/components/schedule/*.tsx` (5파일) |
| 12 | 스케줄 페이지 + loading | 신규 | `src/app/(dashboard)/schedule/page.tsx`, `loading.tsx` |
| 13 | FullCalendar 다크모드 CSS | 수정 | `src/app/globals.css` |
| 14 | 사이드바 메뉴 수정 | 수정 | `src/lib/constants.ts` |
| 15 | 빌드 검증 | - | `npx next build` |
| 16 | 브라우저 테스트 + QA | - | Chrome DevTools MCP |

**총 파일 수**: 신규 17 + 수정 4 = **21개 파일**

---

## 9. 검증 체크리스트

### 킥오프
- [ ] "프로젝트" 유형 프로젝트 생성 시 킥오프 자동 생성 (status: draft)
- [ ] 미니프로젝트/셀프프로젝트 → 킥오프 탭 미표시
- [ ] 킥오프 개요 편집 + 저장
- [ ] 체크리스트 추가/체크/삭제/드래그 정렬
- [ ] 체크리스트 담당자 배정 + 기한 설정
- [ ] 미팅 기록 (참석자/결정사항/노트) 저장
- [ ] 전체 체크리스트 완료 → "킥오프 완료" 버튼 활성화
- [ ] 킥오프 완료 후 태스크 생성 가능
- [ ] 킥오프 미완료 시 태스크 생성 차단 + 안내 메시지

### 스케줄
- [ ] 일정 생성 (종일/시간 지정)
- [ ] 카테고리별 색상 캘린더 표시
- [ ] 월/주/일 뷰 전환
- [ ] 카테고리 필터 on/off
- [ ] "내 일정만 보기" 토글
- [ ] 본인 일정만 수정/삭제 가능
- [ ] 대표/관리자는 모든 일정 수정/삭제 가능
- [ ] 사이드바에 "일정" 메뉴 표시

### 공통
- [ ] 라이트/다크 모드 정상
- [ ] 모바일(375px) 반응형 정상
- [ ] `npx next build` 성공
- [ ] 문제 0건까지 수정-테스트 루프
